<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!--App Launch Buttons Style -->
    <style name="AppLaunchButton">
        <item name="android:layout_marginTop">@dimen/app_launch_button_margin</item>
        <item name="android:layout_marginBottom">@dimen/app_launch_button_margin</item>
        <item name="android:minWidth">@dimen/app_launch_button_width</item>
        <item name="android:typeface">serif</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

</resources>
